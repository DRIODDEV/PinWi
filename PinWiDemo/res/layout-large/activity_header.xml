<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_color" >

    <ImageView
        android:id="@+id/image_header_bg"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/header_bg" />

    <TextView
        android:id="@+id/text_profileHeader"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dip"
        android:text="@string/parent_header_text"
        android:textColor="@color/font_white_color"
        android:textSize="22sp" />

    <ImageView
        android:id="@+id/image_ProfileIcon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/image_header_bg"
        android:layout_centerHorizontal="true"
        android:src="@drawable/child_activityicon" />

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_marginRight="20dip"
        android:layout_marginTop="25dip" >

        <ImageView
            android:id="@+id/image_pinwiIcon_header"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/pinwi_smallicon" />

        <ListView>
        </ListView>
    </RelativeLayout>

</RelativeLayout>